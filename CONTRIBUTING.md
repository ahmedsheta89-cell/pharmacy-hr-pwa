# المساهمة في Pharmacy People

نرحب بالمساهمات التي تحافظ على **سلامة بيانات الموظفين** ووضوح التجربة العربية على الويب والهاتف.

## سير العمل المقترح

ابدأ بإنشاء فرع واضح الاسم من `main`، ثم نفذ تغييراً صغيراً ومركزاً. اكتب رسالة commit تصف الأثر الفعلي للتغيير، ولا تخلط إصلاحات غير مرتبطة في طلب دمج واحد.

قبل فتح Pull Request، شغّل الأوامر التالية:

```bash
pnpm check
pnpm test
pnpm build
```

يجب أن تتضمن التغييرات الوظيفية اختباراً مناسباً، وأن تراعي أحجام الشاشات الهاتفية وسطح المكتب، والاتجاه RTL، وتفضيل `prefers-reduced-motion` للحركة الجديدة.

## قواعد البيانات والصلاحيات

أي تعديل على المخطط يبدأ بتحديث `drizzle/schema.ts` ثم توليد ومراجعة الترحيل قبل تطبيقه. لا تعتمد على الواجهة وحدها للصلاحيات؛ يجب فرض نطاق الفرع والدور داخل إجراءات الخادم. لا تضف بيانات عملاء أو أسرار أو نسخ قواعد بيانات إلى المستودع.

## جودة Pull Request

اشرح في الوصف المشكلة والحل وطريقة الاختبار، وأضف لقطات للشاشات إذا تغيرت الواجهة. استخدم لغة عربية واضحة في النصوص الموجهة للمستخدم، وحافظ على اتساق مصطلحات الصيدلية والموارد البشرية.
