# خطة تحديث التبعيات عالية المخاطر

## نتيجة الفحص الحالية

أُجري فحص إنتاج محلي في 22 أغسطس 2026. المسارات ذات الأولوية في التقرير الحالي هي:

| الحزمة | المسار | مستوى ملحوظ | المعالجة المقترحة |
|---|---|---:|---|
| `fast-xml-parser` | اعتماد إنتاج مباشر، الإصدار 5.3.5 | عالٍ | التحديث إلى `5.3.6` أو أحدث ثم تشغيل الاختبارات والبناء. |
| `express` → `qs` و`path-to-regexp` و`body-parser` | اعتماد غير مباشر من Express 4.21.2 | منخفض إلى عالٍ | ترقية Express ضمن مسار توافق منفصل، مع اختبار مصادقة وtRPC ورفع الملفات. |
| `recharts` → `lodash` | اعتماد غير مباشر من Recharts | عالٍ | فحص نسخة Recharts المتوافقة وتحديثها مع اختبار لوحات KPI. |
| `drizzle-orm` | اعتماد مباشر | عالٍ | مراجعة نسخة الإصلاح والتحديث بعد اختبار الاستعلامات والترحيلات. |
| `nanoid` | اعتماد مباشر | عالٍ | تحديث محدود مع اختبار توليد الرموز ومسارات QR. |

يشير تقرير الفحص إلى عدم وجود مخاطر حرجة، لكن ما زالت توجد مخاطر عالية تتطلب تحديثات تدريجية. تُجرى التحديثات واحدة أو في مجموعات متوافقة، ويتبع كل منها فحص الأنواع والاختبارات وبناء الإنتاج وتدقيق جديد للتبعيات.
