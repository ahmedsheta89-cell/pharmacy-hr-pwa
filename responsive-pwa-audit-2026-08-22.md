# تدقيق الاستجابة وPWA — 22 أغسطس 2026

## المعايير المرجعية

يعتمد التدقيق على [WCAG 2.2 — حجم هدف التفاعل الأدنى](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html)، وعلى إرشادات [web.dev لتطبيقات الويب التقدمية](https://web.dev/learn/pwa/progressive-web-apps) و[قابلية التثبيت](https://web.dev/articles/install-criteria)، وعلى مرجع [MDN للتصميم المتجاوب](https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/CSS_layout/Responsive_Design).

## النتائج قبل التحسين

تمت معاينة المسارات الأساسية على سطح المكتب (1280×720) والهاتف (375×812). التخطيط المكتبي واضح، والتنقل الجانبي RTL منظم، فيما ظهر أن عناصر النماذج والسجلات على الهاتف تتطلب تقليل كثافتها وتحسين سلوك التحميل. إعداد viewport يمنع التكبير بسبب `maximum-scale=1`، وهو غير مناسب للإتاحة. كما أن Service Worker الحالي يخزن غلافاً محدوداً فقط ولا يقدم مساراً صريحاً لاسترداد التنقل دون اتصال، وبيانات manifest تقتصر على أيقونة SVG واحدة.

## نطاق التحسين

سيُزال منع التكبير، وتُضاف مساحات آمنة للهاتف، وتُصقل أهداف اللمس والحالة الحركية، ويُقوى تخزين PWA واسترداد التنقل. ستتضمن عملية التحقق معاينات متجاوبة وفحصاً للأنواع والاختبارات وبناء الإنتاج.

## التحقق بعد التحسين

تمت معاينة صفحة الرواتب في نافذة هاتف قياسية **375×812** بعد تطبيق احتواء الحاوية. كما أجري قياس مباشر عبر Chromium على المسار `/payroll` وكانت النتيجة `window.innerWidth = 375` و`document.documentElement.scrollWidth = 375` و`document.body.scrollWidth = 375`؛ لذلك لا يوجد تمرير أفقي فعلي في شاشة الرواتب ضمن هذا العرض. وللتأكد من المحتوى التفاعلي المخفي أولياً، فُتح نموذج **هيكل الراتب** داخل جلسة التطبيق الموثقة وقيست عناصره الفعلية: `11` عنصر تحكم ظاهر، و`document/body scrollWidth = 360` ضمن نافذة `375`، و`overflowing = []`. بذلك بقيت حقول النموذج وأزراره ضمن العرض ولم تُقص عناصر تفاعلية. أضيف اختبار دائم يحمي حاوية الرواتب من قيود العرض الثابتة، وتبقى معاينة الواجهة المرئية جزءاً من تحقق الإصدار قبل النشر.
